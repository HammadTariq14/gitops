# Dev environment overrides for hello-world-frontend
# CI pipeline updates image.tag on every successful build.

image:
  tag: latest   # updated by CI: do not edit manually

config:
  config.js: 'window.__APP_CONFIG__ = { API_URL: "https://dev-apis.ampliorinternal.com" };'
