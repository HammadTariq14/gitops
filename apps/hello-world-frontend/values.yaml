# Base values for hello-world-frontend
# These are shared across all environments.
# Environment-specific overrides live in values-<env>.yaml

app:
  name: hello-world-frontend
  component: frontend

namespace: dev

image:
  repository: ampliordevacr.azurecr.io/hello-world-frontend
  pullPolicy: IfNotPresent
  tag: latest

replicaCount: 1

service:
  type: ClusterIP
  port: 80
  targetPort: 80

healthCheck:
  liveness:
    path: /
    initialDelaySeconds: 10
    periodSeconds: 30
    timeoutSeconds: 5
    failureThreshold: 3
  readiness:
    path: /
    initialDelaySeconds: 5
    periodSeconds: 10
    timeoutSeconds: 5
    failureThreshold: 3
  startup:
    path: /
    initialDelaySeconds: 5
    periodSeconds: 10
    timeoutSeconds: 5
    failureThreshold: 10

resources:
  limits:
    cpu: 250m
    memory: 256Mi
  requests:
    cpu: 100m
    memory: 128Mi

autoscaling:
  enabled: false

ingress:
  enabled: false

gatewayApi:
  enabled: true
  parentRefs:
    - name: apisix
      namespace: ingress-apisix
  hostnames:
    - dev-frontend.ampliorinternal.com
